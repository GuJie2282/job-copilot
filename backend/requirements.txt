# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# Security & Auth
passlib[bcrypt]==1.7.4
bcrypt<4.0.0  # passlib 兼容性要求
python-jose[cryptography]==3.3.0
python-dotenv==1.0.0

# Rate limiting
slowapi==0.1.9

# Email (reserved for production)
# fastapi-mail==1.4.1

# Pydantic (FastAPI and LangGraph dependency)
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.1.0

# Data validation
email-validator==2.1.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.26.0

# Utils
python-dateutil==2.8.2
json-repair>=0.30.0  # 容错修复 LLM 输出的轻微 JSON 格式错误（缺逗号/引号转义等）

# Resume Parsing
pdfplumber==0.10.3
PyPDF2==3.0.1
python-docx==1.1.0

# LangChain & LLM
langchain==0.3.10
langchain-openai==0.2.10
langchain-core==0.3.28
langgraph==0.2.53
langgraph-checkpoint-sqlite==2.0.11  # 模拟面试会话状态持久化（抗重启）；pin 2.x 兼容 langgraph 0.2.53

# RAG 面经库检索（阶段 7）
# - 嵌入：智谱 embedding-3 API（复用 langchain-openai 的 OpenAIEmbeddings + LLM_API_KEY）
# - 关键词检索：rank_bm25；向量检索：numpy 余弦相似度（embedding 存 SQLite JSON 列）
# - 不用 chromadb：其依赖 chroma-hnswlib 在 Windows 需 MSVC 编译，对部署不友好；
#   面经库数据量极小（百级），numpy 检索足够且更透明（诚实的技术判断，design 原则 3）
rank_bm25>=0.2.2

# Task Scheduling (for file cleanup)
apscheduler==3.11.0
