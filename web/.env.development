# API Base URL
# 用相对路径 /api，走 Vite dev proxy（见 vite.config.ts 的 server.proxy: /api → localhost:8001）
# 这样本地开发 / cpolar 内网穿透公网访问都能通；
# 不要写死 http://localhost:8001——朋友浏览器里的 localhost 是他自己的电脑，连不到你本机
VITE_API_BASE_URL=/api

# SSE 流式端点专用：直连后端，绕开 Vite dev proxy
# 原因：vite proxy 不流式转发 text/event-stream（150s 透传 0 字节，连接挂起）。
# 普通 JSON 请求（history/enrich 等）仍走 VITE_API_BASE_URL 的 proxy；只有流式 match 直连。
# 后端 CORS 已放行 localhost:3000，浏览器直连 8001 可正常流式。
# （cpolar 公网测试若要流式，需把此项指到公网后端地址）
VITE_STREAM_BASE_URL=http://localhost:8001/api

# App Config
VITE_APP_TITLE=求职 Copilot
